#include <bits/stdc++.h>
using namespace std;
#define PI 3.14 // Using macro function will not take any space.
int main()
{
    int r = 5;

    // It will take some space which will decrease speed in very small amount
    // double pi=3.14
    // double area=pi*r*r;

    double area = PI * r * r;
    cout << "Area of circle is -> " << area << endl;
}